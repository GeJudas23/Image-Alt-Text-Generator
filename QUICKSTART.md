# Quick Start Guide

Быстрый запуск расширения Firefox Image Alt Text Generator за 5 минут.

## Шаг 1: Установите зависимости

```bash
# Установите Python зависимости
pip install -r requirements.txt

# Установите web-ext (опционально, для разработки)
npm install -g web-ext
```

## Шаг 2: Запустите API сервер

```bash
# Запустите пример API сервера
python example_api_server.py
```

Сервер запустится на `http://localhost:8000`

**Важно:** Это MOCK сервер для тестирования. Он возвращает случайные описания.
Для реального использования замените его на API с AI моделью (OpenAI Vision, Claude, etc.)

## Шаг 3: Загрузите расширение в Firefox

### Вариант A: Временная загрузка (для тестирования)

1. Откройте Firefox
2. Введите в адресной строке: `about:debugging#/runtime/this-firefox`
3. Нажмите **"Load Temporary Add-on..."**
4. Выберите файл `manifest.json` в папке проекта
5. Расширение загружено! (активно до перезапуска Firefox)

### Вариант B: Через web-ext (рекомендуется)

```bash
# Перейдите в папку проекта
cd d:\Python\FireFox_Image_to_text

# Запустите расширение
web-ext run
```

Firefox откроется автоматически с установленным расширением.

## Шаг 4: Протестируйте расширение

### Тест 1: Откройте тестовую страницу

```bash
# Откройте test.html в браузере
# Или просто перетащите файл в Firefox
```

URL: `file:///d:/Python/FireFox_Image_to_text/test.html`

### Тест 2: Откройте реальный сайт

Попробуйте:
- https://en.wikipedia.org/wiki/Cat
- https://unsplash.com/
- Любой сайт с изображениями

### Тест 3: Проверьте консоль

1. Откройте Developer Tools (F12)
2. Перейдите на вкладку **Console**
3. Вы должны увидеть логи:
   ```
   [ImageAlt:Content] === Image Alt Text Generator Extension Started ===
   [ImageAlt:ImageProcessor] Found 10 meaningful images out of 25 total
   [ImageAlt:APIClient] API success: Фотография пейзажа...
   ```

## Шаг 5: Проверьте результаты

### Через консоль браузера:

```javascript
// Получить статистику
window.imageAltExtension.getTrackerStats()
// → { processedCount: 15, inFlightCount: 0 }

window.imageAltExtension.getAPIStats()
// → { total: 15, success: 15, failed: 0 }
```

### Визуально:

Наведите курсор на изображения - всплывающая подсказка (title) покажет сгенерированный alt текст.

## Частые проблемы

### ❌ "Extension not detected"

**Решение:**
1. Убедитесь, что расширение загружено (`about:debugging`)
2. Обновите страницу (Ctrl+R)
3. Проверьте консоль на ошибки

### ❌ "API request failed"

**Решение:**
1. Убедитесь, что API сервер запущен: `python example_api_server.py`
2. Проверьте доступность: откройте `http://localhost:8000` в браузере
3. Должно быть: `{"status":"running",...}`

### ❌ Изображения не обрабатываются

**Возможные причины:**
1. Изображения меньше 100x100px (фильтруются)
2. Изображения в `<nav>`, `<header>`, `<footer>` (фильтруются)
3. API сервер не запущен
4. Проверьте консоль: `window.imageAltExtension.getAPIStats()`

### ❌ CORS ошибки

**Это нормально!** Расширение автоматически обходит CORS через background script.
Вы можете видеть warning в консоли, но изображение всё равно обработается.

## Следующие шаги

### 1. Интеграция с реальным AI

Замените `example_api_server.py` на интеграцию с:
- **OpenAI Vision API**
- **Claude with Vision**
- **Google Cloud Vision**
- **Azure Computer Vision**
- Любой другой AI Vision API

Пример с OpenAI:

```python
from openai import OpenAI

client = OpenAI(api_key="your-api-key")

@app.post("/analyze")
async def analyze_image(image: UploadFile = File(...)):
    image_data = await image.read()
    base64_image = base64.b64encode(image_data).decode('utf-8')

    response = client.chat.completions.create(
        model="gpt-4-vision-preview",
        messages=[{
            "role": "user",
            "content": [
                {"type": "text", "text": "Describe this image in Russian"},
                {"type": "image_url", "image_url": {
                    "url": f"data:image/jpeg;base64,{base64_image}"
                }}
            ]
        }]
    )

    return {"description": response.choices[0].message.content}
```

### 2. Настройте фильтры

Отредактируйте параметры в [content/imageProcessor.js](content/imageProcessor.js):

```javascript
const MIN_IMAGE_WIDTH = 150;  // Увеличить минимальный размер
const MIN_IMAGE_HEIGHT = 150;
```

### 3. Оптимизируйте производительность

Отредактируйте [utils/apiClient.js](utils/apiClient.js):

```javascript
const MAX_CONCURRENT_REQUESTS = 5;  // Больше параллельных запросов
const BATCH_DELAY_MS = 200;         // Меньше задержка
```

### 4. Отключите отладочные логи

В каждом `.js` файле установите:

```javascript
const DEBUG = false;  // Отключить логи
```

## Готово! 🎉

Расширение работает и обрабатывает изображения автоматически.

Для дополнительной информации см. [README.md](README.md)
